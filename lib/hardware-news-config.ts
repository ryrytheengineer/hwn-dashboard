/** How often feeds are refetched (server cache + client poll stay aligned). */
export const HARDWARE_NEWS_INTERVAL_SECONDS = 3600;

export const HARDWARE_NEWS_POLL_MS = HARDWARE_NEWS_INTERVAL_SECONDS * 1000;
